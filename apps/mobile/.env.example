# Use your machine's LAN IP (not localhost) so a phone on the same
# network can reach the local Supabase instance.
EXPO_PUBLIC_SUPABASE_URL=http://192.168.1.10:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-supabase-start
